# Contributing

Start with `python -m unittest discover -s tests -v`. Keep runtime dependencies at zero unless a concrete
use case justifies one. Include a minimal fixture, expected behavior and a regression test
for bug fixes. Avoid submitting real secrets, personal data or proprietary reports.

Open a focused issue before a large feature. Describe your OS, runtime version, command,
exit status and expected result. This project does not promise response times.
